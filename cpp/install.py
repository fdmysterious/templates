#!/usr/bin/env python
import os

# Getting app name ( by default current folder name )
#appName      = os.path.basename( os.path.dirname( __file__ ) )
#appNameInput = input( 'App name ? [{0}] : '.format(appName) )
#if appNameInput : appName = appNameInput

#appDesc = input( 'App desc : ' )

# Modifying files #
# End of section #
